'use strict';

/**
 * event-age-restriction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::event-age-restriction.event-age-restriction');
